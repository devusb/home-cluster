apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bichon
  namespace: bichon
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: bichon
      port:
        name: http
  tls:
    - hosts:
        - bichon
