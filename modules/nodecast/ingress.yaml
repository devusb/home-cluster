apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nodecast
  namespace: nodecast
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: nodecast
      port:
        name: http
  tls:
    - hosts:
        - nodecast
