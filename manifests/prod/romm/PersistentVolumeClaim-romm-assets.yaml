apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: romm-14.12.0
    app.kubernetes.io/instance: romm
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: romm
    app.kubernetes.io/version: 4.3.0
    helm-revision: "1"
    helm.sh/chart: romm-14.12.0
    release: romm
  name: romm-assets
  namespace: romm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
